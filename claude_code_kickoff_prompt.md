# Claude Code 전달용 프롬프트

아래 내용을 그대로 복사해서 Claude Code에 붙여넣으시면 됩니다.

---

## 프로젝트 개요

**프로젝트명**: SentinelPose (가칭 — CCTV 이상행동 탐지 + RAG 매뉴얼 연동)

**목표**: 이직 준비용 포트폴리오 프로젝트. 기존에 만든 Qwen 라벨링 + KLUE-BERT 뉴스 분류 + RAG 검색 파이프라인과 이어지는 두 번째 프로젝트로, 영상처리(Temporal 처리) 역량을 증명하는 것이 핵심 목표.

**핵심 아키텍처 (2-Stage)**
1. Stage 1: YOLOv8-Pose + 내장 tracker(`model.track(persist=True, tracker="bytetrack.yaml")`)로 사람 키포인트 시퀀스 추출
2. 전처리: 키포인트 결측치 보간(confidence < 0.3 → NaN → linear interpolation) + BBox 기준 상대좌표 정규화 (`x_norm = (x-x_min)/w`, `y_norm = (y-y_min)/h`)
3. Stage 2: 경량 1D-CNN 또는 단일 레이어 GRU로 행동 분류 (입력 shape: `Batch, Channel=51(17 keypoints x,y,conf), Length=30frames`)
4. RAG 연동: 이상 이벤트 발생 시 `{"event": "...", "time": "...", "location": "..."}` 메타데이터 생성 → 기존 RAG 벡터 DB 검색 → 대응 매뉴얼 반환

**인프라**
- 로컬: RTX 4060 Ti 8GB (개발/디버깅용)
- 학습: Kaggle 무료 GPU (주 30시간, T4x2 또는 P100) — Save & Run All 커밋 방식으로 무인 실행
- Kaggle 디스크 제약: 20GB (입력 데이터 용량이 실질 병목)

**일정**: 8~10일 (Ultra-Lean 5~7일 옵션도 검토했으나 디버깅 버퍼 확보를 위해 8~10일로 확정)

**데이터**
- AI Hub "이상행동 CCTV 영상" (NIA2019 Database) 사용
- 원본은 4K(3840x2160) 해상도, 클립당 3~4분 길이, 파일당 약 385MB
- 클래스: 폭행(assault)/침입/쓰러짐(falldown) 등 중 3개 선정
- XML 라벨에 프레임 단위 이벤트 구간(예: punching 2181~2274프레임)과 action별 세부 라벨이 포함되어 있음
- 다운로드는 클래스당 10~12개(총 30~36개, 약 11~14GB)로 제한 후, 로컬에서 이벤트 구간만 트리밍 + 저해상도 다운샘플링하여 Kaggle에는 전처리된 경량 버전만 업로드할 계획

---

## 현재 상황 및 도움이 필요한 부분

1. **[진행 중] AI Hub 데이터 다운로드**: 승인은 완료됐고 다운로드 프로그램(ExAgent/INNORIX 계열) 설치를 시도 중인데, 설치 파일을 실행해도 반응 없이 넘어가기만 하는 문제가 있음. 관리자 권한 실행, SmartScreen, 백신 차단 등 기본적인 것들은 확인했지만 해결이 안 됨. **가능하면 AI 허브 오픈 API + WSL 경로로 우회하는 방법을 같이 검토해줘** — API 다운로드는 분할 압축(zip.part*)으로 오고 리눅스 명령어로 병합해야 한다는 것까지는 알고 있음.

2. **전처리 파이프라인 스캐폴딩**: 데이터가 아직 없어도 먼저 작성 가능한 부분부터 시작하고 싶음
   - FFmpeg 기반 트리밍 스크립트 (XML의 start/end frame 기준으로 이벤트 구간 + 전후 여유 1~2초 잘라내기)
   - 해상도 다운샘플링 스크립트 (4K → 720p 또는 480p)
   - 키포인트 결측치 보간 함수 + 단위 테스트
   - BBox 상대좌표 정규화 함수 + 단위 테스트
   - 1D-CNN / GRU 모델 클래스 정의 (PyTorch)

3. **프로젝트 구조 설계**: 전체 디렉토리 구조와 Kaggle 노트북에서 재사용 가능한 모듈 구조를 잡고 싶음

이 중에서 지금 바로 시작할 수 있는 것부터 진행해줘. AI Hub 다운로드 문제는 별도 트랙으로 같이 조사해주면 좋겠고, 나머지 코드 작업을 우선순위대로 진행해줬으면 해.
