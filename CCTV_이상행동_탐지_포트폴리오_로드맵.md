# CCTV 이상행동 탐지 포트폴리오 프로젝트 로드맵

## 프로젝트 개요
- **목표**: 영상처리 핵심 역량을 증명하는 포트폴리오 프로젝트, 기존 RAG 파이프라인과 연동
- **일정**: 8~10일 (디버깅 버퍼 포함)
- **핵심 아키텍처**: YOLOv8-Pose(내장 tracker) → 키포인트 전처리 → 1D-CNN/GRU 경량 분류기 → RAG 매뉴얼 검색 연동
- **인프라**: 로컬 RTX 4060 Ti 8GB(개발/디버깅) + Kaggle(주 30시간 GPU, 본 학습)

---

## Day 0 (최우선) — AI Hub 데이터 신청

**왜 가장 먼저인가**: AI Hub는 데이터 다운로드 전 승인 절차가 있고, 보통 1~3일(경우에 따라 더 길게) 소요됩니다. 이 단계가 늦어지면 이후 모든 일정이 그대로 밀리는 구조라, 다른 작업(코드 설계, 환경 세팅)을 하더라도 신청서는 오늘 바로 넣어두는 게 맞습니다.

- [ ] AI Hub 회원가입/기업·개인 인증 확인
- [ ] "이상행동 CCTV 영상" 계열 데이터셋 검색 및 신청
- [ ] 승인 대기 중 Day 1~2 작업(환경 세팅, 코드 스캐폴딩)을 병행
- [ ] 승인 지연 대비 플랜 B 확보: Kaggle의 "UCF-Crime" 등 공개 CCTV 이상행동 데이터셋을 백업으로 조사해두기

---

## Day 1~2 — 데이터 스코프 확정 및 전처리 파이프라인

**설명**: Kaggle 디스크 제약(20GB)이 실질적 병목이므로, 처음부터 전체 데이터를 받지 않고 필요한 클래스·클립만 선별합니다.

- 클래스 3개 선정 (예: 쓰러짐 / 침입 / 폭력)
- 클래스당 20~30개, 총 60~100개 클립으로 스코프 제한
- OpenCV/Decord로 프레임 추출 스크립트 작성
- 선별된 클립을 Kaggle Custom Dataset으로 업로드

---

## Day 3 — Pose 추출 및 품질 점검

**설명**: 여기서 결측치 비율을 먼저 확인해야, Day 4 전처리 설계가 헛수고가 되지 않습니다.

- YOLOv8-Pose `model.track(persist=True, tracker="bytetrack.yaml")`로 키포인트 시퀀스 덤프 (JSON/NPY)
- confidence < 0.3 키포인트 비율 확인 → 결측 패턴 파악
- 트래킹 ID 끊김(occlusion으로 인한 재할당) 여부 샘플 점검

---

## Day 4 — 전처리 핵심: 결측 보간 + 좌표 정규화

**설명**: 이 프로젝트에서 가장 중요한 단계입니다. 여기가 부실하면 이후 모델 성능이 전처리 노이즈에 발목 잡힙니다.

1. **결측치 보간**: conf < 0.3 → NaN 처리 → `interpolate(method='linear')`로 선형 보간, 장기 결측은 (0,0,0) 마스킹
2. **BBox 상대좌표 정규화**: `x_norm = (x - x_min) / w`, `y_norm = (y - y_min) / h` → [0,1] 범위, 카메라 거리/각도 불변성 확보

---

## Day 5 — 분류기 학습 및 검증

**설명**: 소규모 데이터셋이므로 복잡한 모델(ST-GCN 등)은 지양하고, 1D-CNN 또는 단일 레이어 GRU로 빠르게 수렴시킵니다.

- 입력 shape: `(Batch, Channel=51, Length=30frames)`
- 1D-CNN 또는 GRU로 학습 (수 분 내 수렴 예상)
- 단순 Accuracy 대신 **Confusion Matrix** 및 클래스 간 오분류 양상 분석

---

## Day 6 — 실시간 통합 추론 디버깅

- YOLOv8 tracker + 1D-CNN 분류기를 하나의 추론 파이프라인으로 통합
- Latency(FPS) 측정 → 포트폴리오 지표로 활용

---

## Day 7 — RAG 연동

**설명**: 기존 KLUE-BERT/RAG 프로젝트의 벡터 DB를 재활용해 "이상 이벤트 → 대응 매뉴얼 검색" 흐름을 붙입니다.

- 이상 탐지 시 `{"event": "Fall_Down", "time": "...", "location": "..."}` 형태 메타데이터 생성
- 메타데이터 기반 RAG 검색 → 대응 매뉴얼/보고서 양식 반환

---

## Day 8 — UI/시각화

- Streamlit 또는 Gradio로 기본 대시보드 구성
- 비디오 재생 + BBox/Pose 오버레이 + 이상 탐지 타임라인 + RAG 매뉴얼 팝업

---

## Day 9~10 — 패키징 및 문서화

- 전체 파이프라인 통합 디버깅
- 데모 GIF/영상 캡처
- README: 아키텍처 다이어그램, 의사결정 근거(왜 2-Stage인지, 왜 1D-CNN인지), Confusion Matrix, Latency 수치 포함

---

## 핵심 리스크 체크리스트
- [ ] AI Hub 승인 지연 → 백업 데이터셋 확보해둘 것
- [ ] Kaggle 디스크 20GB 제한 → 클립 수 미리 제한
- [ ] Pose 결측치 처리 누락 → Day 4 필수 단계
- [ ] 좌표 정규화 누락 → 일반화 성능 저하 원인
- [ ] 소규모 데이터셋의 성능 과신 → Confusion Matrix + 정성적 데모로 포지셔닝
